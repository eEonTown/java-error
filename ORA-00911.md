> ORA-00911

### 원인
에러가 발생한 쿼리문을 DB에 실행시키면 실행이 잘 되는데
서버구동시 오류가 발생했습니다.
<br>
원인은 ;(세미콜론)이었습니다.
제 작업 방식은 DB에서 쿼리문을 작성 후 문제가 없다면 복사해서 사용하는 방식인데
세미콜론도 같이 복사를 해오는 바람에 오류가 발생했습니다.

<br>

### 해결
쿼리문 복사 시 세미콜론을 한 번 더 확인합니다.